# CR-001: `<一句话标题>`

CR 是 Change Request，用于维护期中等规模行为变更。

## 1. 元数据

| 字段 | 值 |
| --- | --- |
| CR 编号 | `CR-001` |
| 状态 | `drafting` |
| 创建日期 | `YYYY-MM-DD` |
| 关联 INBOX | `INBOX-001` |
| 关联 Sprint | `<规划后填写>` |

## 2. 背景与需求

```text
<用户原话或测试发现摘录>
```

`<客观需求摘要，不写实现方案>`

## 3. 范围

### 范围内

- `<必须改的行为、接口、配置或文档契约>`

### 范围外

- `<明确不做的事项>`

## 4. 验收标准

每条必须是可观测行为。

- [ ] AC1: `<输入/步骤 -> 可观察输出/错误/状态>`

## 5. 影响与实施记录

| 路径 | 影响类型 | 摘要 |
| --- | --- | --- |
| `<path>` | `新增/修改/删除` | `<摘要>` |

## 6. 收尾判定

- `<PASS/HAS_FAIL>`
