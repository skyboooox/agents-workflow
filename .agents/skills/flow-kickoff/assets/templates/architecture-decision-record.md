# 架构决策记录

日期：`<YYYY-MM-DD>`

## 背景

`<为什么需要这个决策>`

## 决策

`<最终采用什么方案>`

## 方案对比

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| `<方案>` | `<优点>` | `<缺点>` | `<采用/放弃原因>` |

## 模块边界

- `L1_Entry`：`<入口>`
- `L2_Core`：`<流程编排>`
- `L3_Business`：`<业务能力>`
- `L4_Atom`：`<底层能力>`

## 数据流

```text
<输入> -> <处理> -> <输出>
```

## 依赖方向

- `<模块 A> -> <模块 B>`

## 风险

- `<风险>`：`<缓解方式>`

## 验证方式

- `<验证步骤>`

## 回滚或调整路径

- `<如何撤回或演进>`
