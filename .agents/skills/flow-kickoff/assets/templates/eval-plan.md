# 评估计划

用于复杂任务、Agent 工作流、工具调用或高风险变更。只写能判断通过/失败的用例，不写长测试说明。

| ID | 契约 | 场景 | 输入 | 期望结果 | 评分逻辑 | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `E-001` | `C-001` | `<happy/edge/failure/regression>` | `<输入或步骤>` | `<可观察结果>` | `<自动/人工判断规则>` | `<命令、日志、截图、文件>` | `<planned/pass/fail/blocked>` |

## 维护规则

- 每个关键 `contract_id` 至少有一个验证入口。
- 失败路径和权限边界必须有用例。
- 评分逻辑必须可执行或可人工复核。
- 不把完整日志写入本文件；只写证据位置。
